# G2E P5A D2 — Terminal Stop Decision

## Status

**D2 research line:** CLOSED  
**Model-bearing successor:** NOT AUTHORIZED  
**Reason:** unresolved terminal-failure mechanism after exhaustive allowed existing-evidence recovery

## Chain

1. **D2-S2** — formal-close preturn instrument component skew; no scientific turn consumed.
2. **D2-S3** — consumed scientific attempt; INVALID due client queue observation defect
   `UNCAUGHT_QUEUE_EMPTY_IN_RPC_CLIENT_NEXT_MESSAGE`.
3. **D2-S4** — queue-safe successor; consumed attempt; reached real terminal `failed`; no result.
4. **D2-S4 post-closure bundle** — decisive `TurnError` payload was not preserved by the sanitizer.
5. **D2-S4 persisted local recovery** — zero matching text/session/log records and zero matching SQLite
   rows for the exact thread/turn IDs.

## Decision

The D2 scientific line stops here.

`NO_D2S5_AUTHORIZATION`

No additional D2 model turn, retry, timeout adjustment, alternate harness, permission adjustment,
provider change, or speculative protocol repair is authorized from the current evidence.

## What remains valid

- the D2-S3 queue-failure mechanism attribution;
- the D2-S4 proof that the queue-safe repair reaches a genuine terminal notification;
- structural official-harness qualification at its previously scoped level;
- all frozen evidence and formal-close lineage.

## Future admissible direction

A future program may investigate **prospective observability / instrumentation adequacy** independently
of D2 outcomes, for example ensuring that terminal `TurnError` mechanism fields are preserved in a
privacy-safe evidence channel before any scientific task is dispatched.

That future program must have a new program identity and must first qualify its instrumentation without
running P5-FX-001 or another outcome-bearing task.

This document does not open such a program.
